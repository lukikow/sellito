%p
  = link_to 'Back', categories_path, class: 'btn'
  = link_to 'Edit', edit_category_path(@category), class: 'btn'
  = link_to 'Delete', @category, method: :delete, data: { confirm: "Are you sure?"}, class: 'btn'

%h2= @category.name
